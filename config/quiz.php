<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 1セクションあたりの問題数上限
    |--------------------------------------------------------------------------
    |
    | 要件定義3.2: 回答は常にセクション内の全問一括で行うため、1リクエストで
    | 一括採点できる問題数(最大10問)を超えて問題を作らせない。
    | テストでは env() で小さい値に差し替えて境界値検証を高速化する。
    |
    */
    'max_questions_per_section' => (int) env('MAX_QUESTIONS_PER_SECTION', 10),

    /*
    |--------------------------------------------------------------------------
    | 回答履歴の保持件数
    |--------------------------------------------------------------------------
    |
    | 要件定義3.4: 同一問題については直近10件のみ回答(answers)を保持し、
    | それより古いものは自動削除する。
    |
    */
    'answer_retention_limit' => (int) env('ANSWER_RETENTION_LIMIT', 10),

];
